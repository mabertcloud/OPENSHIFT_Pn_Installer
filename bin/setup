#!/bin/bash -e

PATH=/bin/:/usr/bin:$PATH

source $OPENSHIFT_CARTRIDGE_SDK_BASH

case "$1" in
  -v|--version)
    version="$2"
esac

client_result "Setup called $version"

#cp -r $OPENSHIFT_PLN_DIR/versions/$version/* $OPENSHIFT_PLN_DIR/

exit 0
